/** @type {import('next').NextConfig} */
const nextConfig = {
    // output: 'export',
    output: 'export',
    reactStrictMode: true,
    compiler: {
        styledComponents: true,
    },
};

export default nextConfig;
